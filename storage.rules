rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // SQUAD document uploads: authenticated users may read/write their own files.
    match /cases/{uid}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
