# QA-only Python dependencies (pinned for deterministic validation)
# Install into the repo venv:
#   C:/Projects/SQUAD/.venv/Scripts/python.exe -m pip install -r tools/qa/requirements.txt

jsonschema==4.25.1
